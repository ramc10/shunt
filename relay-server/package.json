{
  "name": "shunt-relay-server",
  "version": "1.0.0",
  "description": "shunt Tier-2 relay — aggregates request history from multiple shunt instances into Postgres",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "db:migrate": "bun src/db.ts"
  },
  "dependencies": {
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
